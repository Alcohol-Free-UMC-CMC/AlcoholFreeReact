import React from 'react';
import { LogoContainer } from "./LogoButton.style";

const LogoButton = () => {
  return (
    <LogoContainer>
      
    </LogoContainer>
  );
};

export default LogoButton;